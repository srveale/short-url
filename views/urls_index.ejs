<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% include partials/_header %>
    <ul>
      <% for(var url in urls) { %>
        <li><%= url + ': ' + urls[url] %></li>
      <% } %>
    </ul>
</body>
</html>